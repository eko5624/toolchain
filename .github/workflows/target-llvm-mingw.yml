name: Build target-llvm-mingw-ucrt-x86_64
defaults:
  run:
    shell: bash
on:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: docker://ghcr.io/eko5624/archlinux:latest
    steps:    
      - name: Checkout
        uses: actions/checkout@main
        
      - name: Setup Cross Toolchain
        run: |
          curl -OL https://github.com/${{ github.repository }}/releases/download/toolchain/llvm-mingw-20240125-ucrt-x86_64.7z
          7z x llvm-mingw*.7z
          rm llvm-mingw*.7z
          
      - name: Loading target llvm cache
        uses: actions/cache/restore@main
        with:
          path: ${{ github.workspace }}/target
          key: target-llvm-mingw-${{ github.run_id }}
          restore-keys: |
            target-llvm-mingw-
            
      - name: Building toolchain
        run: |
          if [[ ! "$(ls -A target/bin)" ]]; then
            chmod +x ./target-llvm-mingw.sh
            ./target-llvm-mingw.sh
          fi

      - name: Get current timestamp
        run: echo "date=$(date +%Y%m%d)" >> $GITHUB_ENV

      - name: Packaging toolchain
        run: |
          7z a -r -l target-llvm-mingw-${{ env.date }}-ucrt-x86_64.7z target/*

      - name: Release
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }} 
        run: |
          chmod +x ./package-release.sh
          ./package-release.sh

      - name: Saving llvm cache
        uses: actions/cache/save@main
        if: always()
        with:
          path: ${{ github.workspace }}/target
          key: target-llvm-mingw-${{ github.run_id }}
