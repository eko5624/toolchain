name: target-llvm-mingw
defaults:
  run:
    shell: bash
on:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: docker://ghcr.io/eko5624/archlinux:latest
    steps:    
      - name: Checkout
        uses: actions/checkout@main
        
      - name: Setup Cross Toolchain
        run: |
          #curl -OL https://github.com/${{ github.repository }}/releases/download/toolchain/cross-llvm-mingw-2023-12-12-ucrt-x86_64.7z
          curl -OL https://github.com/${{ github.repository }}/releases/download/toolchain/llvm-mingw-2023-12-28-ucrt-archlinux-x86_64.7z
          7z x llvm-mingw*.7z
          rm llvm-mingw*.7z
          rm cross/x86_64-w64-mingw32/lib/libclang_rt.builtins-x86_64.a
          rm cross/x86_64-w64-mingw32/lib/libstdc++.a
          
      - name: Loading target llvm cache
        uses: actions/cache/restore@main
        with:
          path: ${{ github.workspace }}/target
          key: target-llvm-${{ github.run_id }}
          restore-keys: |
            target-llvm-
            
      - name: Building toolchain
        run: |
          PWD=$(pwd)
          if [[ ! "$(ls -A $PWD/target/bin)" ]]; then
            chmod +x ./target-llvm-mingw.sh
            ./target-llvm-mingw.sh
          fi

      - name: Get current timestamp
        run: echo "date=$(date +%Y-%m-%d)" >> $GITHUB_ENV  

      - name: Packaging toolchain
        run: |
          7z a -r -l target-llvm-mingw-${{ env.date }}-ucrt-x86_64.7z target/*

      - name: Release
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }} 
        run: |
          chmod +x ./release-package.sh
          ./release-package.sh

      - name: Saving llvm cache
        uses: actions/cache/save@main
        if: always()
        with:
          path: ${{ github.workspace }}/target
          key: target-llvm-${{ github.run_id }}
